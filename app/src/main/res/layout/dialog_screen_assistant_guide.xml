<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/dialogRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:background="@android:color/transparent">

    <LinearLayout
        android:id="@+id/dialogContainer"
        android:background="@drawable/bg_dialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="10dp"
        android:paddingTop="10dp"
        android:paddingEnd="10dp"
        android:paddingBottom="26dp">

        <TextView
            android:id="@+id/dialogTitle"
            app:fontFamily="@font/lexend_semibold"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enable Screen Assistant"
            android:textColor="@color/visa_brown"
            android:textSize="28sp" />

        <TextView
            android:id="@+id/dialogMessage"
            app:fontFamily="@font/lexend_regular"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="26dp"
            android:textSize="21sp"
            android:lineSpacingExtra="6dp"
            android:textColor="@color/visa_brown"
            android:text="To read information from other app screens, please turn on the accessibility service.&#10;&#10;Steps:&#10;1. Tap VisA Screen Assistant in Accessibility settings.&#10;2. Turn on the main switch at the top.&#10;3. Tap Allow.&#10;&#10;You do not need to turn on the shortcut option.&#10;&#10;After enabling it, return to this app." />

        <Button
            android:id="@+id/btnOpenSettings"
            app:backgroundTint="@null"
            android:background="@drawable/bg_dialog_button"
            app:fontFamily="@font/lexend_semibold"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="30dp"
            android:text="Open Settings"
            android:textSize="20sp"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/btnCancel"
            app:backgroundTint="@null"
            android:background="@drawable/bg_dialog_button"
            app:fontFamily="@font/lexend_semibold"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="12dp"
            android:text="Cancel"
            android:textSize="20sp"
            android:textAllCaps="false" />

    </LinearLayout>
</ScrollView>